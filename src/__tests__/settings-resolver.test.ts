// import { assert } from "chai";

// import { resolveSettings } from "../resolvers/settings-resolver";

// describe("Settings Resolver", () => {
//   it("should correctly load a default setting", () => {
//     const settings = resolveSettings({});
//     assert.strictEqual(settings.provider, "shields");
//   });
//   it("should correctly override a default setting", () => {
//     const config = { settings: { provider: "someoneElse" } };
//     const settings = resolveSettings(config);
//     assert.strictEqual(settings.provider, "someoneElse");
//   });
// });
